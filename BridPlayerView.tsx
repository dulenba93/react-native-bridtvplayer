import {
  PixelRatio,
  UIManager,
  findNodeHandle,
  requireNativeComponent,
} from 'react-native';

import PropTypes from 'prop-types';

//ovo verovatno treba da bude intex.js u modulu
// import {BridPlayerViewManager} from './BridPlayerViewManager';

export default requireNativeComponent(
  'BridtvplayerView'
);